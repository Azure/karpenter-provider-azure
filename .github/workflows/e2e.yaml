name: E2E
on:
  workflow_call:
    inputs:
      git_ref:
        type: string
      suite:
        type: string
        required: true
      location:
        type: string
        description: "the azure location to run the e2e test in"
        default: "westus3"
      provision_mode:
        type: string
        description: "the karpenter provisioning mode to run the e2e test in"
        default: "aksscriptless"
      k8s_version:
        type: string
        description: "the kubernetes version to use (e.g., 1.30). If empty, uses AKS default."
      azure_vm_size:
        type: string
        description: "the azure vm size to use for the e2e test (set to empty string to allow AKS to default)"
        default: "Standard_D4ds_v5"  # We have more quota for this VM size so force it to avoid quota errors
    secrets:
      E2E_SUBSCRIPTION_ID_TEST:
        required: true
      E2E_CLIENT_ID_TEST:
        required: true
  workflow_dispatch:
    inputs:
      suite:
        type: choice
        required: true
        description: "Test suite to run"
        options:
          - ACR
          - BYOK
          - Chaos
          - Consolidation
          - Drift
          - GPU
          - InPlaceUpdate
          - Integration
          - KubernetesUpgrade
          - Machine
          - NewSKUs
          - NodeClaim
          - Scheduling
          - Spot
          - Storage
          - Subnet
          - Utilization
      location:
        type: choice
        description: "Azure location to run the e2e test in"
        default: westus3
        # Limiting to a subset of regions to give a nice dropdown and also
        # ensure the regions are configured to pass with all of our tests (capacity, etc)
        options:
          - westus3
          - southcentralus
          - eastus
          - westus2
          - eastus2euap
          - centraluseuap
      k8s_version:
        type: choice
        description: "Kubernetes version ('default' = AKS default)"
        options:
          - "default"
          - "1.30"
          - "1.31"
          - "1.32"
          - "1.33"
          - "1.34"
          - "1.35"
          - "1.36"
      provision_mode:
        type: choice
        description: "Karpenter provisioning mode"
        default: aksscriptless
        options:
          - aksscriptless
          - aksmachineapi
          - aksmachineapiheaderbatch
      azure_vm_size:
        type: string
        description: "the azure vm size to use for the e2e test (set to empty string to allow AKS to default)"
        default: "Standard_D4ds_v5"  # We have more quota for this VM size so force it to avoid quota errors

permissions:
  contents: read

env:
  ACR_NAME:
  RG_NAME:
  CLUSTER_NAME:

jobs:
  run-suite:
    name: suite-${{ inputs.suite }}
    runs-on: karpenter-ci-1es-pool
    permissions:
      contents: read
      statuses: write # ./.github/actions/commit-status/*
    env:
      AZURE_SUBSCRIPTION_ID: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
      AZURE_CLIENT_ID: ${{ secrets.E2E_CLIENT_ID_TEST }}
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          disable-telemetry: true
          egress-policy: block
          # - wildcards to allow for variation in targeted clusters
          # - clients3.google.com:80 and firebaselogging-pa.googleapis.com:443 - confirmed Skaffold,
          #   likely telemetry, likely can be avoided/blocked with/after "skaffold config set --global collect-metrics false"
          allowed-endpoints: >
            *.azmk8s.io:443
            *.azurecr.io:443
            *.data.mcr.microsoft.com:443
            *.vault.azure.net:443
            aka.ms:443
            api.github.com:443
            app.aladdin.microsoft.com:443
            auth.docker.io:443
            azcliextensionsync.blob.core.windows.net:443
            clients3.google.com:80
            dc.services.visualstudio.com:443
            distroless.dev:443
            firebaselogging-pa.googleapis.com:443
            gist.githubusercontent.com:443
            github.com:443
            graph.microsoft.com:443
            index.docker.io:443
            login.microsoftonline.com:443
            management.azure.com:443
            mcr.microsoft.com:443
            objects.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            proxy.golang.org:443
            golang.org:443
            raw.githubusercontent.com:443
            storage.googleapis.com:443
            sum.golang.org:443
            go.dev:443
            dl.google.com:443
            aquasecurity.github.io:443
            mirror.gcr.io:443
            azure.archive.ubuntu.com:80
            packages.microsoft.com:443
            esm.ubuntu.com:443
            169.254.169.254:80

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.git_ref }}
      - if: always() && github.event_name == 'workflow_run'
        uses: ./.github/actions/commit-status/start
        with:
          name: ${{ github.workflow }} / e2e (${{ inputs.suite }})
          git_ref: ${{ inputs.git_ref }}
      - uses: ./.github/actions/install-deps
      - name: az login
        shell: bash
        run: az login --identity --client-id "$AZURE_CLIENT_ID"
      - name: az set sub
        shell: bash
        run: az account set --subscription "$AZURE_SUBSCRIPTION_ID"
      - name: install skaffold
        run: |
          curl -Lo skaffold https://github.com/GoogleContainerTools/skaffold/releases/download/v2.14.2/skaffold-linux-amd64 --fail
          install skaffold /usr/local/bin/
      - name: generate rg name
        env:
          SUITE: ${{ inputs.suite }}
        run: |
          RG_NAME=$(echo karpenter-e2e-"$SUITE"-${{ github.run_id }} | awk '{print tolower($0)}')
          echo "Using resource group name \"$RG_NAME\""
          echo "RG_NAME=$RG_NAME" >> "$GITHUB_ENV"
      - name: generate acr name
        env:
          SUITE: ${{ inputs.suite }}
        run: |
          ACR_NAME=$(echo "$SUITE"acr${{ github.run_id }} | awk '{print tolower($0)}')
          echo "Using acr name \"$ACR_NAME\""
          echo "ACR_NAME=$ACR_NAME" >> "$GITHUB_ENV"
      - name: cleanup leftover resources, if any (e.g. from a previous run that failed to clean up)
        # Could reuse the existing cleanup action, but would require a bit of refactoring (e.g. it uses non-blocking delete)
        run: az group delete --name "$RG_NAME" --yes 2>/dev/null || true
      - name: create acr '${{ env.ACR_NAME }}'
        uses: ./.github/actions/e2e/create-acr
        with:
          subscription_id: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
          resource_group: ${{ env.RG_NAME }}
          acr_name: ${{ env.ACR_NAME }}
          git_ref: ${{ inputs.git_ref }}
          location: ${{ inputs.location }}
      - name: add jitter on cluster creation
        run: |
          # Creating jitter so that we can stagger cluster creation to avoid throttling
          sleep $(( RANDOM % 60 + 1 ))
      - name: generate cluster name
        run: |
          CLUSTER_NAME=mc
          echo "Using cluster name \"$CLUSTER_NAME\""
          echo "CLUSTER_NAME=$CLUSTER_NAME" >> "$GITHUB_ENV"
      - name: create aks cluster '${{ env.CLUSTER_NAME }}'
        uses: ./.github/actions/e2e/create-cluster
        with:
          subscription_id: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
          resource_group: ${{ env.RG_NAME }}
          cluster_name: ${{ env.CLUSTER_NAME }}
          acr_name: ${{ env.ACR_NAME }}
          git_ref: ${{ inputs.git_ref }}
          location: ${{ inputs.location }}
          k8s_version: ${{ inputs.k8s_version }}
          provision_mode: ${{ inputs.provision_mode }}
          identity_type: ${{ inputs.suite == 'Machine' && 'UserAssigned' || 'SystemAssigned' }}
          azure_vm_size: ${{ inputs.azure_vm_size }}
      - name: install karpenter
        uses: ./.github/actions/e2e/install-karpenter
        with:
          subscription_id: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
          resource_group: ${{ env.RG_NAME }}
          cluster_name: ${{ env.CLUSTER_NAME }}
          acr_name: ${{ env.ACR_NAME }}
          git_ref: ${{ inputs.git_ref }}
          location: ${{ inputs.location }}
          provision_mode: ${{ inputs.provision_mode }}
      - name: run the ${{ inputs.suite }} test suite
        env:
          AZURE_CLUSTER_NAME: ${{ env.CLUSTER_NAME }}
          AZURE_RESOURCE_GROUP: ${{ env.RG_NAME }}
          AZURE_LOCATION: ${{ inputs.location }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
          AZURE_ACR_NAME: ${{ env.ACR_NAME }}
          AZURE_CLIENT_ID: ${{ secrets.E2E_CLIENT_ID_TEST }}
          TEST_SUITE: ${{ inputs.suite }}
          GIT_REF: ${{ github.sha }}
          PROVISION_MODE: ${{ inputs.provision_mode }}
        run: |
          make az-creds
          make e2etests
      - name: dump logs on failure
        uses: ./.github/actions/e2e/dump-logs
        if: failure() || cancelled()
        with:
          subscription_id: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
          resource_group: ${{ env.RG_NAME }}
          cluster_name: ${{ env.CLUSTER_NAME }}
          git_ref: ${{ inputs.git_ref }}
      - name: cleanup resources
        uses: ./.github/actions/e2e/cleanup
        if: always()
        with:
          subscription_id: ${{ secrets.E2E_SUBSCRIPTION_ID_TEST }}
          resource_group: ${{ env.RG_NAME }}
          cluster_name: ${{ env.CLUSTER_NAME }}
          acr_name: ${{ env.ACR_NAME }}
          git_ref: ${{ inputs.git_ref }}
      - if: always() && github.event_name == 'workflow_run'
        uses: ./.github/actions/commit-status/end
        with:
          name: ${{ github.workflow }} / e2e (${{ inputs.suite }})
          git_ref: ${{ inputs.git_ref }}
